'use strict';
describe('util', () => {
	beforeEach(async () => {
	});

	afterEach(async () => {
		// sinon.restore();
	});

	it('provides findDeviceOsDirectory()');
	it('provides execCommand()');
	it('provides delay()');
	it('provides findTestName()');
	it('provides shortenRight()');
});
