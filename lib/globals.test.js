'use strict';
describe('globals.js', () => {
	beforeEach(async () => {
	});

	afterEach(async () => {
		// sinon.restore();
	});

	it('provides expect');
	it('provides platform');
	it('provides systemMode');
	it('provides systemThread');
	it('provides tag');
	it('provides fixture');
	it('provides timeout');
});
