'use strict';
// const { expect } = require('chai');
// const { SpecError, RunnerError, InternalError } = require('./error');
describe('Errors', () => {
	beforeEach(async () => {
	});

	afterEach(async () => {
		// sinon.restore();
	});

	it('provides SpecError class');
	it('provides RunnerError class');
	it('provides InternalError class');
	it('provides isInternalError()');

});
