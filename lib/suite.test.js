'use strict';
describe('PlatformSuite', () => {
	beforeEach(async () => {
	});

	afterEach(async () => {
		// sinon.restore();
	});

	it('provides init()');
	it('provides shutdown()');
	it('provides runTest()');
});
