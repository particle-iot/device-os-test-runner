// const config = require('./config');
describe('config', () => {
	beforeEach(async () => {
	});

	afterEach(async () => {
		// sinon.restore();
	});

	it('provides .showUsage()');
	it('provides .loadCliProfile()');
	it('provides .parseCmdLine()');
	it('provides .updateConfig()');
	it('provides .loadConfigFiles()');
	it('provides .initConfig()');
});
